import { useState } from 'react';import MicForm from '../components/MicForm';import MicTable from '../components/MicTable';
export default function MicPlot({mics,setMics,makeId}){const [form,setForm]=useState({});const [f,setF]=useState(''); const add=()=>{setMics([{id:makeId(),...form},...mics]);setForm({});}; const rows=mics.filter(m=>Object.values(m).join(' ').toLowerCase().includes(f.toLowerCase())); return <section><h2>Mic Plot</h2><input className='filter' placeholder='Filter by show, character, scene, status' value={f} onChange={e=>setF(e.target.value)}/><MicForm form={form} setForm={setForm} onAdd={add}/><MicTable rows={rows}/></section>}
