import { exportCsv } from '../services/csvExport';import { formatReportCopy } from '../services/reportFormatter';
export default function Exports({cues,mics,propsRows,reports}){const copy=async()=>navigator.clipboard.writeText(formatReportCopy(reports[0]));return <section><h2>Exports</h2><div className='grid-2'><article className='card'><button onClick={()=>window.print()}>Print Cue Sheet</button><button onClick={()=>exportCsv(cues,'cue-sheet.csv')}>Export Cue Sheet CSV</button><button onClick={()=>exportCsv(mics,'mic-plot.csv')}>Export Mic Plot CSV</button><button onClick={()=>exportCsv(propsRows,'props.csv')}>Export Props CSV</button><button onClick={copy}>Copy Rehearsal Report</button></article><article className='card'><h3>Google Drive export coming soon</h3><p>Direct folder sync and auto-versioning will land in an upcoming release.</p></article></div></section>}
