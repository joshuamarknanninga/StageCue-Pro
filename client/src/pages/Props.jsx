import { useState } from 'react';import PropForm from '../components/PropForm';import PropTable from '../components/PropTable';
export default function Props({propsRows,setPropsRows,makeId}){const [form,setForm]=useState({});const [f,setF]=useState(''); const add=()=>{setPropsRows([{id:makeId(),...form},...propsRows]);setForm({});}; const rows=propsRows.filter(p=>Object.values(p).join(' ').toLowerCase().includes(f.toLowerCase())); return <section><h2>Props</h2><input className='filter' placeholder='Filter by show, scene, act, status' value={f} onChange={e=>setF(e.target.value)}/><PropForm form={form} setForm={setForm} onAdd={add}/><PropTable rows={rows}/></section>}
