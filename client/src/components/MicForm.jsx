export default function MicForm({form,setForm,onAdd}){const ch=e=>setForm({...form,[e.target.name]:e.target.value}); return <form className="form card" onSubmit={e=>{e.preventDefault();onAdd();}}>{['show','actorName','character','micNumber','transmitterPack','sceneUsage','muteNotes','backupMic','status'].map(k=><input key={k} name={k} placeholder={k} value={form[k]||''} onChange={ch} required={k==='show'||k==='actorName'}/>) }<button>Add Mic Assignment</button></form>;}
