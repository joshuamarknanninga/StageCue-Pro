export default function PropForm({form,setForm,onAdd}){const ch=e=>setForm({...form,[e.target.name]:e.target.value}); return <form className="form card" onSubmit={e=>{e.preventDefault();onAdd();}}>{['show','propName','act','scene','presetLocation','responsible','spikeTapeNote','checkInStatus'].map(k=><input key={k} name={k} placeholder={k} value={form[k]||''} onChange={ch} required={k==='show'||k==='propName'}/>) }<button>Add Prop Preset</button></form>;}
